<div class="container">
  <p>Nouveau projet</p>

  <%= simple_form_for @home_new do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <% (1..12).each do |month| %>
      <%= f.input "home_consumption_months[]", input_html: { name: "home[home_consumption_months][#{month}]", value: nil }, label: "Month #{month}" %>
    <% end %>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
  <% end %>
</div>
